[{
  "position": "top",
  "modules-left": ["sway/workspaces", "sway/mode", "sway/window"],
  "modules-center": ["custom/calendar", "custom/times", "custom/weather"],
  "modules-right": ["custom/videorec", "custom/gifrec", "custom/vpn", "network", "pulseaudio", "backlight", "idle_inhibitor", "battery"],

  "sway/workspaces": {
    "disable-scroll": true,
    "format": "{icon}",
  },

  "sway/mode": { "format": " {}", "max-length": 50 },
  "sway/window": { "format": "{}", "max-length": 50 },

  "custom/calendar": {
    "format": " {}",
    "on-click": "alacritty --title Calcurse -e calcurse",
    "exec": "$HOME/.config/waybar/modules/cal.py",
    "interval": 3600,
    "return-type": "json",
  },
  "custom/times": {
    "format": " {}",
    "exec": "$HOME/.config/waybar/modules/times.py",
    "interval": 60,
    "return-type": "json",
  },
  "custom/weather": {
    "format": "{}",
    "tooltip": true,
    "on-click": "alacritty --title Weather -e weather",
    "interval": 3600,
    "exec": "$HOME/.config/waybar/modules/wttr.py",
    "return-type": "json"
  },

  "custom/vpn": {
    "format": "VPN ",
    "exec": "echo '{\"class\": \"connected\"}'",
    "exec-if": "test -d /proc/sys/net/ipv4/conf/tun0",
    "return-type": "json",
    "interval": 5
  },

  "custom/videorec": {
    "format": " VID",
    "exec": "exec record-screend",
    "exec-on-event": false,
    "on-click": "pkill -INT -P \"$(pgrep -xo record-screen)\" wf-recorder"
  },

  "custom/gifrec": {
    "format": " GIF",
    "exec": "exec record-gifd",
    "exec-on-event": false,
    "on-click": "pkill -INT -P \"$(pgrep -xo record-gif)\" wf-recorder"
  },

  "network": {
    "tooltip": true,
    "format": "{iframe}",
    "format-wifi": "{icon} {signalStrength}%",
    "tooltip-format-wifi": "Inrterface: {ifname}\nIP: {ipaddr}/{cidr}\nSSID: {essid}\nFreq: {frequency} MHz\nStrength: {signaldBm} dBm ({signalStrength}%)\nUp: {bandwidthUpBits}\nDown: {bandwidthDownBits}",
    "format-ethernet": "{icon}",
    "tooltip-format-ethernet": "Inrterface: {ifname}\nIP: {ipaddr}/{cidr}\nUp: {bandwidthUpBits}\nDown: {bandwidthDownBits}",
    "format-disconnected": "",
    "format-icons": { "wifi": "", "ethernet": "" },
    "on-click": "alacritty -e nmtui"
  },

  "pulseaudio": {
    "format": "{icon} {volume:2}% {format_source}",
    "format-bluetooth": "{icon} {volume}%  {format_source}",
    "format-muted": "{icon} --% {format_source}",
    "format-icons": {
      "headphones": "",
      "hands-free": "",
      "headset": "",
      "phone": "",
      "portable": "",
      "car": "",
      "default": ""
    },
    "scroll-step": 1,
    "on-click": "pamixer -t",
    "on-click-right": "alacritty -e ncpamixer",
    "format-source": "",
    "format-source-muted": ""
  },

  "backlight": {
    "format": "{icon} {percent}%",
    "format-icons": ["","","","","","",""],
    "on-scroll-down": "light -U 1",
    "on-scroll-up": "light -A 1"
  },

  "idle_inhibitor": {
    "format": "{icon}",
    "format-icons": {
      "activated": "",
      "deactivated": ""
    }
  },

  "battery": {
    "states": { "good": 95, "warning": 30, "critical": 15 },
    "format": "{icon} {time}",
    "format-charging": " {capacity}%",
    "format-plugged": "",
    "format-icons": ["", "", "", "", "", "", "", "", "", "", ""],
  },
}, {
  "position": "bottom",
  "modules-left": ["custom/scratchpad-indicator", "memory", "cpu", "temperature", "disk"],
  "modules-center": ["custom/cmus"],
  "modules-right": ["custom/updates", "custom/updates-aur", "custom/tasks", "custom/quit"],

  "custom/scratchpad-indicator": {
    "interval": 3,
    "return-type": "json",
    "exec": "swaymsg -t get_tree | jq --unbuffered --compact-output '( select(.name == \"root\") | .nodes[] | select(.name == \"__i3\") | .nodes[] | select(.name == \"__i3_scratch\") | .focus) as $scratch_ids | [..  | (.nodes? + .floating_nodes?) // empty | .[] | select(.id |IN($scratch_ids[]))] as $scratch_nodes | { text: \"\\($scratch_nodes | length)\", tooltip: $scratch_nodes | map(\"\\(.app_id // .window_properties.class) (\\(.id)): \\(.name)\") | join(\"\\n\") }'",
    "format": "{}",
    "on-click": "exec swaymsg 'scratchpad show'",
    "on-click-right": "exec swaymsg 'move scratchpad'"
  },
  "memory": { "interval": 1, "format": " {}% ({used:0.2f}G)" },
  "cpu": { "interval": 5, "format": " {usage:2}%" },
  "temperature": { "format": " {temperatureC}°C" },
  "disk": { "interval": 5, "format": " {percentage_used:2}%", "path": "/" },
  "custom/bluetooth": {
    "format": "{}",
    "return-type": "json",
    "exec": "$HOME/.config/waybar/modules/bluetooth.py",
    "interval": 3600,
  },

  "custom/cmus": {
    "format": "♪ {}",
    "max-length": 75,
    "interval": 10,
    "exec": "cmus-remote -C \"format_print '%a - %t'\"",
    "exec-if": "pgrep cmus",
    "on-click": "cmus-remote -u",
    "escape": true
  },

  "custom/updates": {
    "format": " {}",
    "format-none": "",
    "escape": true,
    "return-type": "json",
    "exec": "checkupdates | $HOME/.config/waybar/modules/counter.sh",
    "exec-if": "checkupdates",
    "interval": 3600,
    "signal": 8,
    "on-click": "alacritty -e paru && pkill -SIGRTMIN+8 waybar",
  },
  "custom/updates-aur": {
    "format": " {}",
    "format-none": "",
    "escape": true,
    "return-type": "json",
    "exec": "paru -Qua | $HOME/.config/waybar/modules/counter.sh",
    "exec-if": "paru -Qua",
    "interval": 3600,
    "signal": 8,
    "on-click": "alacritty -e paru && pkill -SIGRTMIN+8 waybar",
  },
  "custom/tasks": {
    "format": " {}",
    "return-type": "json",
    "exec": "$HOME/.config/waybar/modules/tasks.py",
    "interval": 3600,
  },
  "custom/quit": {
    "format": "{}",
    "return-type": "json",
    "exec": "$HOME/.config/waybar/modules/quit.py",
    "interval": 3600,
  },
}]
