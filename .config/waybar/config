{
	"position": "top",

	"modules-left": [
		"sway/workspaces",
		"sway/window",
	],
	"modules-center": [
		"clock#1",
		"clock#2"
	],
	"modules-right": [
    "custom/updates",
    "network#1",
    "network#2",
		"pulseaudio",
		"memory",
		"cpu",
		"temperature",
		"disk",
		"battery",
    "idle_inhibitor",
	],

  "idle_inhibitor": {
    "format": "{icon}",
    "format-icons": {
      "activated": "",
      "deactivated": ""
    }
  },

  "custom/updates": {
    "format": " {}",
    "exec": "checkupdates | wc -l",
    "interval": 30
  },

  "network#1": {
    "interface": "tun0",
    "format": "{icon} ON",
    "format-wifi": "{icon} ON",
    "format-ethernet": "{icon} ON",
    "format-disconnected": "{icon} OFF",
    "format-icons": {
      "wifi": ["聯", "聯", "聯"],
      "ethernet": ["聯"],
      "disconnected": ["輦"]
    },
    "tooltip-format": "{bandwidthUpBits} Up {bandwidthDownBits} Down",
    "tooltip-format-disconnected": "VPN Disconnected",
    "interval": 30
  },

  "temperature": {
    "format": " {temperatureC}°C"
  },

  "network#2": {
    "format": "{icon} {ifname}",
    "format-alt": "{ipaddr}/{cidr} {icon}",
    "format-alt-click": "click-right",
    "format-icons": {
      "wifi": ["直", "直" ,"直"],
      "ethernet": [""],
      "disconnected": [""]
    },
    "on-click": "alacritty -e nmtui",
    "tooltip": false
  },

	"sway/workspaces": {
		"disable-scroll": true,
		"format": "{icon}",
    "format-icons": {
      "1": " code",
      "2": " web",
      "3": " design",
      "4": " term",
      "5": " music",
      "6": " -",
      "7": " -",
      "urgent": ""
    }
	},

  "sway/window": {
    "format": "{}",
    "max-length": 50
  },

	"clock#1": {
		"format": " {:%a, %d/%m/%Y}",
    "tooltip-format": "{calendar}"
	},

	"clock#2": {
		"format": " {:%H:%M}"
	},

	"pulseaudio": {
		"format": "{icon} {volume:2}%",
		"format-bluetooth": "{icon} {volume}% ",
		"format-muted": "MUTE",
		"format-icons": {
			"headphones": "",
			"default": [
				"",
				""
			]
		},
		"scroll-step": 5,
		"on-click": "pamixer -t",
		"on-click-right": "pavucontrol"
	},
	"memory": {
		"interval": 5,
		"format": " {}%"
	},
	"cpu": {
		"interval": 5,
		"format": " {usage:2}%"
	},
	"battery": {
		"states": {
			"good": 95,
			"warning": 30,
			"critical": 15
		},
		"format": "{capacity}% {icon}",
    "format-alt": "{time} {icon}",
    "format-alt-click": "click-right",
		"format-icons": ["", "", "", "", ""]
	},
	"disk": {
		"interval": 5,
		"format": " {percentage_used:2}%",
		"path": "/"
	}
}
