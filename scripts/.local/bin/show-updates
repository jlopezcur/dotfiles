#!/bin/sh

arch=$(checkupdates)
[[ -n $arch ]] && echo -e "Arch:\n-----\n$arch\n"

aur=$(paru -Qua)
[[ -n $aur ]] && echo -e "AUR:\n----\n$aur\n"

[[ -z $arch && -z $aur ]] && echo -e "Nothing to update!\n"

read -p "Press Enter to continue" </dev/tty
